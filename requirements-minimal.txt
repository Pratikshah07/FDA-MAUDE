# Minimal dependencies for Vercel deployment (core Flask app only)
# This excludes IMDRF Insights feature to stay under 250MB limit

flask>=3.0.0
flask-login>=0.6.3
werkzeug>=3.0.1
itsdangerous>=2.1.2
python-dotenv>=1.0.0

# Firebase Authentication (token verification via PyJWT + Google public keys)
PyJWT>=2.8.1
cryptography>=41.0.7

# Essential for MAUDE processing
pandas==2.0.3
openpyxl>=3.1.2
numpy==1.24.3

# Note: IMDRF Insights feature will be disabled on Vercel due to size constraints
# For full functionality including insights, run locally or use a different platform
